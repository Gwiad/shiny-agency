import ThemeButton from '.';
import { render } from '@testing-library/react';

describe('ThemeButon', () => {
  it('renders Theme Button', () => {
    render(<ThemeButton />);
  });
  it('does stuff', () => {});
});
